# eFinder Apache configuration
# Extends the connection timeout so the MJPEG stream in stream.php
# is not killed by Apache's default 60-second TimeOut.
# Installed to /etc/apache2/conf-available/efinder.conf
# and enabled with: a2enconf efinder

TimeOut 3600
